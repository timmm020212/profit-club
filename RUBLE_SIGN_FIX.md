# ✅ **Исправлен знак рубля**

## 🎯 **Проблема решена:** Вместо "руб" теперь используется знак ₽

## 🔧 **Что изменено:**

### **1. На сайте (`BookingServiceCard.tsx`):**
```typescript
{price && (
  <span className="text-lg font-semibold text-[#2A2A2A] whitespace-nowrap tabular-nums">
    {price}
    <span className="text-sm text-[#2A2A2A]/70 ml-1">₽</span>
  </span>
)}
```

### **2. В админке (`AdminSiteServicesManager.tsx`):**
```typescript
<span className="absolute right-4 top-1/2 transform -translate-y-1/2 text-gray-400 pointer-events-none">
  {draft.price ? "₽" : ""}
</span>
```

### **3. Валидация обновлена:**
```typescript
// Проверяем цену - убираем "₽" если есть и проверяем что остались только цифры
const priceWithoutSuffix = next.price.replace(/[₽rub]/gi, "").trim();
```

## 💡 **Как теперь выглядит:**

### **На сайте:**
```
1500₽   [Записаться]
```

### **В админке:**
```
[1500     ₽]
```

## 🎨 **Преимущества знака ₽:**

- ✅ **Профессионально** - официальный знак рубля
- ✅ **Компактно** - занимает меньше места
- ✅ **Современно** - соответствует стандартам
- ✅ **Универсально** - понятен всем

## 🧪 **Как проверить:**

### **1. На сайте:**
```
http://localhost:3000/booking
```
Цена должна выглядеть: "1500₽"

### **2. В админке:**
```
http://localhost:3000/admin/site
```
Поле цены должно показывать "₽" справа

### **3. Создание услуги:**
- Введите "1500" → увидите "1500₽"
- Сохранится только "1500" в БД

## ✅ **Результат:**

- ✅ **Сайт:** "1500₽" вместо "1500 руб"
- ✅ **Админка:** "₽" вместо "руб"
- ✅ **Валидация:** Работает с знаком ₽
- ✅ **Сохранение:** В БД только числа

**Теперь везде используется правильный знак рубля ₽!** 🎉
